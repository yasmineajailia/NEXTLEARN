shap>=0.52
scikit-learn>=1.7
pandas>=2.0
numpy>=1.26
fastapi>=0.115
pydantic>=2.7
uvicorn[standard]>=0.30
joblib>=1.3
requests>=2.31
python-dotenv>=1.0
# RAG (Phase A): vector store + course-file text extraction
chromadb>=1.5
pypdf>=6.0
python-docx>=1.1
python-pptx>=1.0
# NLI entailment verifier (answer-gap pipeline).
# Plain `pip install` pulls the CUDA-bundled torch wheel (~2-3GB). On a
# CPU-only server install it from PyTorch's CPU index instead — Dockerfile.ml
# does this automatically; for a manual/venv setup run this BEFORE the file:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu "torch>=2.2"
torch>=2.2
transformers>=4.40
